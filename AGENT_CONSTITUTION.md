# 记忆学习平台 — Agent 宪法

> 本宪法是编码时必须遵守的**最高规则**。架构优先、设计优先、真源优先、严格验收。
> 版本：v3.1 | 生成日期：2026-08-18（本版新增：7.3系统化调试 / 9.1子代理契约 / 十二章交接模板 / 附录A技能最佳实践）

---

## 适配标识

```
@@PROJECT:记忆学习平台（Memory Learning Platform）@@
@@TRUTH:daima/CLAUDE.md + daima/word/PROJECT_CHARTER.md@@
@@STACK:Vue 3.4 + TypeScript 5.4 + Vite 5 / Node 20 + Express 4 + better-sqlite3 / Pinia 2 + Vue Router 4 + Axios + Tiptap@@
@@COMMAND:npm run dev / npm run build / tsc --noEmit / npm test@@
@@BOUNDARY:goal=笔记管理+间隔重复记忆卡片的单用户本地Web应用；non-goal=AI生成卡片(M2)、多用户体系(M2)、移动端(M3)、.apkg导入、云同步(M3)@@
```

---

## 一、核心原则

1. **架构优先、设计优先、真源优先、严格验收。** 编码前必须想清楚：为什么这样做、是否正确、是否存在更简单且更符合当前系统的做法。
2. **agent 必须敢于指出不合理的需求、错误的架构方向、冲突的规则和缺失的验收条件**，而不是机械执行。
3. **禁止补丁式开发**：不得用散落 if、临时兼容分支、硬编码、sleep、全局 flag、mock 或假数据绕过架构问题。
4. **禁止无意义向后兼容**：旧字段、旧接口残影若污染当前合同，必须先向用户说明取舍，不得擅自保留双路径。
5. **禁止选项剧场**：用户要求架构或方案时，给出一个推荐主线；备选方案只用于解释为什么不选。
6. **禁止偷懒、糊弄和留尾巴。** 完成定义必须包含可验证结果、未闭合风险和下一步边界。
7. **用户明确否定的概念必须从代码、文档、计划和命名中删除**，禁止换个名字继续保留。
8. **当用户说"先看""不动代码""先给判断"时**，必须保持只读审查，直到用户明确要求执行。
9. **最小改动不是半截改动。** 修 bug 不顺手重构，但本轮触达的调用链必须闭合到 owner、测试和文档。

---

## 二、真源与文档体系

### 2.1 真源优先级（冲突仲裁链）

```
1. 当前源码 / 测试 / 脚本 / schema / 运行日志 / 实机证据 / git 状态
2. 项目根目录宪法文件：CLAUDE.md → AGENTS.md → 等价规则
3. @@TRUTH@@：daima/CLAUDE.md → daima/word/ 六份工作副本
4. 当前 active 的 charter / architecture / implementation plan / acceptance
5. 当前有效的 docs/ / README / issue / commit history
6. archive 历史文档（word/ 归档）→ 仅作模式证据，不覆盖当前事实
```

> 真源缺失、矛盾或明显错误时，先报告冲突 + 证据 + 推荐处理方式，再等用户确认。

### 2.2 文档主次

| 位置 | 角色 | 使用方式 |
|------|------|---------|
| `daima/word/` | 主工作副本（编码依据） | 日常修改回写此目录 |
| `word/` | 归档原件（回顾参考） | 含选型理由；重大技术改变时同步更新 |

### 2.3 冲突仲裁锚点

错误码→STANDARDS §6.2 / SM-2→CHARTER §3.3+ARCH §8 / API→ARCH §6.2 / 表结构→DB §3 / 前端→FRONTEND §3-§6

### 2.4 文档分层（禁止混写）

| 层 | 受众 | 内容 | 位置 |
|----|------|------|------|
| **内部真源** | agent/维护者 | 架构、宪法、owner map、门禁、治理、阶段计划、漂移控制 | @@TRUTH@@ / daima/ |
| **外部用户** | 用户/运维 | 使用、部署、配置、公开 API、集成、排障 | docs/ / README |
| **发布说明** | 发布受众 | changelog、breaking change | RELEASE-NOTES |
| **归档** | 历史参考 | 失效/被吸收/一次性文档 | word/ / archive/ |

- 同一变更同时影响用户行为和内部机制时，**必须分别写入对应层**。
- 新真源文档必须被真源索引引用；完成、失效或一次性文档必须**归档或标注非当前**，不能留在主目录误导。

---

## 三、必需工作流

```
真源审计 → 推理闸 → 唯一 owner 与合同设计 → 测试/fixture/门禁计划
→ 核心实现 → 薄 adapter/UI/API 接线 → 针对性验收
→ 文档回写（分层）→ git 边界复核
```

- **空项目启动**：必须先完成产品定义、唯一主线、第一闭环、技术栈确认和验收标准，禁止直接写代码。
- **半路接管**：必须先做只读接管审计，确认入口、调用链、契约、配置、部署习惯和未提交变更，禁止重写。
- **施工顺序**：严格按 project/IMPLEMENTATION_PLAN.md 的 9 阶段执行，每阶段出口关卡全绿才推进。

---

## 四、推理闸（不闭合，不实现）

1. 实际要解决的问题是什么？
2. 谁创建这个概念，谁调用它，谁消费它？
3. 当前真源在哪里？是否已有同职责模块？
4. 唯一 @@OWNER@@ 是哪一层？哪些层禁止成为 owner？
5. 是否跨多入口？如果跨，语义必须先进共享合同或核心层。
6. 更简单、更保守的设计是什么？为什么不够？
7. 最大回归风险是什么？用什么测试/日志/证据阻断？

---

## 五、设计与安全规则

1. **共享语义单一真源**：跨入口复用的概念进入 @@OWNER@@，不散落在 adapter、controller、UI。
2. **adapter/controller/UI/HTTP/worker 只做协议映射与接线，不拥有核心业务语义。**
3. **已有风格优先复用**；只有现有模式错误或不足时才替换。
4. **抽象只在减少真实复杂度时引入**，禁止"看起来架构化"的过度封装。
5. **生成物只读不手改**：发现 `DO NOT EDIT`、codegen 输出、迁移生成物时，找源文件和生成命令。
6. **外部输入必有上界、背压、释放或降级策略**；禁止无界增长和隐性常驻运行面。
7. **修改后必须同步文档**（分层回写）。
8. **安全**：生产响应绝不泄露 stack trace/文件路径；配置走 `.env`+`config.ts`，禁止硬编码密钥；鉴权（M2）必须在服务端边界执行；`/api/health` 在 authGuard 之前。

---

## 六、错误分级

| 级别 | 定义 | 处理 |
|------|------|------|
| **阻断** | 破坏核心功能、owner 边界、安全/隐私、数据真相、构建测试、关键体验 | 当轮必须收掉 |
| **设计风险** | 导致架构漂移、运行面失控、后续难维护 | 必须说明取舍和验收入口 |
| **可记录债务** | 不影响本轮目标链路 | 必须说明不处理原因和后续入口 |
| **无关优化** | 不进入本轮范围 | 禁止借机扩大改造 |

> 禁止用情绪强度替代工程优先级。无证据担忧不写阻断；有证据阻断也不能降级成"后面再说"。

---

## 七、验收与构建

### 7.1 验收规则（匹配风险，不跑最轻）

1. 必须提供本轮**实际运行过**的命令、测试、日志、截图、构建或设备/用户侧证据。
2. 无法运行关键验收时，说明原因、影响范围和可替代证据，**不能隐瞒**。
3. 失败时：先复现 → 收集证据 → 建立可证伪假设 → 再修改；**禁止无复现地猜测原因**。
4. UI/前端改动：必须检查真实渲染、交互、加载态、错误态、移动/桌面约束；不得只看代码。
5. 性能/安全/数据/权限/并发/迁移/发布改动：**必须扩大验收面**。
6. **警告按缺陷处理**：warning、lint warning、类型告警、格式漂移、生成物漂移、文档索引缺失、本轮 TODO/TBD，除非明确记录为非本轮债务，否则不能带着它们声称完成。
7. **隔离测试不伪装全局事实**：基于缓存、最近窗口、截断列表或有限快照的测试，不能宣称代表全局累计结果。

### 7.2 构建与验收命令

```bash
npm run dev          # 前端 5173 + 后端 3000 + health 检查
npm run build        # 前后端构建
tsc --noEmit         # 类型检查零错误（前后端）
npm test             # 测试套件
```

- 最终以 CHARTER §1.5 的 10 项功能 + 7 项技术验收为准。

### 7.3 系统化调试流程（遇到 bug 时强制走，禁止拍脑袋改代码）

> 灵感来源：Superpowers `systematic-debugging`，融合本宪法第七、六章的证据与分级要求。

**四步法（每步必须有可落盘证据，禁止跨阶段）：**

| 阶段 | 动作 | 输出 / 证据要求 | 不满足禁止进入下一阶段 |
|------|------|----------------|----------------------|
| **① 复现（Reproduce）** | 最小环境复现，记录精确触发条件、输入、期望 vs 实际输出 | `复现步骤 N 步` + 命令行/日志/截图/请求体/响应体，**第三人按步骤可稳定复现** | 无法稳定复现 → 不开始分析 |
| **② 假设（Hypothesize）** | 列 ≥2 个互不重叠的根因假设，每个附带**可证伪实验** | `假设 A / B …` + 每个假设的 `验证方法` + `若为真的可见征兆` + `若为假的排除信号` | 只有 1 个假设 → 回去继续脑暴 |
| **③ 实验（Experiment）** | 按假设逐个做实验，只改与实验直接相关的一行或一处；其余不变 | 每个实验的 `命令` + `输出` + `是否证伪`；同一假设未证伪前不换假设 | **禁止连续两次修改同一处**但不变更假设（等于在赌） |
| **④ 修复与回归（Fix & Regress）** | 确认假设后写修复代码；跑 `tsc --noEmit` + 相关验收 + 复现步骤反向校验 | 修复前的**失败证据** + 修复后的**通过证据** + 本功能未影响其他链路的**回归证据** | 无回归对比不得声称修复完成 |

**调试铁律：**
- 若 ≥3 轮实验全部证伪，回到 ② 重新建假设，禁止原地反复改同一段代码。
- 修到第 ③ 阶段才发现根因跨模块/越 owner 边界时，回到推理闸重新设计，不得就地写补丁。
- 调试过程（步骤、假设、实验记录、结论）写进本轮答复或交接文本；口头/脑内的不算。

---

## 八、Git 规则

1. **提交前确认**：git root、`git status --short`、ignored 文件、staged list、嵌套仓库边界。
2. **禁止 `git add .`**。只 stage 与本任务相关的文件。
3. **禁止 force-add ignored 文件**，除非用户明确点名路径并要求。
4. **dirty worktree 中不得回滚、覆盖或吸入用户未授权的改动**。
5. **嵌套仓库分别处理**：父仓和子仓分别审计、验收、staged、提交。
6. **不使用破坏性 git 命令**（如 `push --force`、`reset --hard`），除非用户明确要求并确认风险。

---

## 九、多 Agent 规则

1. 只在任务能拆成**互不干扰的独立域**时使用多 agent。
2. 每个子 agent 必须有明确范围、输入、禁止事项和期望输出。
3. **关键路径阻塞不能交给子 agent 后原地等待**；主 agent 必须继续做不重叠的工作。
4. 分配不重叠的文件或模块 @@OWNER@@，集成时复核冲突。
5. **子 agent 的结论不是最终真源**；主 agent 必须对照当前代码、测试和文档验收。

### 9.1 子代理任务契约模板（每次派发子代理必须套此模板，禁止只写一句"帮我做 XX"）

> 灵感来源：Superpowers `dispatching-parallel-agents` + `subagent-driven-development`

**派发输入（主 agent 写，子 agent 读）：**

```
━━ 子代理任务契约 ━━
任务 ID：     {短标识，如 sub-review-stats-20260818}
任务类型：     {功能实现 / 修复 bug / 只读审查 / 测试编写 / 文档回写 / 脚手架初始化}
工作范围：     {明确的文件路径或模块列表，如 src/stores/stats.ts + src/views/Stats.vue}
@@OWNER@@：   {本次允许修改的唯一 owner 层；其他层禁止触碰；写"仅 services/"之类}
需求要点：
  1. {用编号列出本次要交付的具体变更点，3-8 条}
  2. …
  3. …
禁止事项（硬红线）：
  ✗ 不得修改 {文件或模块列表}
  ✗ 不得改动 {冻结常量、SM-2、错误码、响应体结构、import 新依赖等}
  ✗ 不得自行新建 {若无授权不得新建文件/新路由/新表/新 store}
验收命令：
  ✓ 必跑：tsc --noEmit；npm run build（若适用）
  ✓ 针对：{本任务专属的验收命令或手工操作步骤，附期望结果}
宪法引用：
  ‣ 本任务必须遵守：AGENT_CONSTITUTION.md 第 X 章、第 Y 节、冻结常量 §Z
  ‣ 设计依据：daima/word/XXX.md §A.B
  ‣ 若冲突，按 @@TRUTH@@ 仲裁链升级，不得自解
输出形式（交付物）：
  - 改了哪些文件（逐条列）
  - 每条变更点对应是否达成（✓/✗ + 原因）
  - 实际跑过的验收命令 + 原始输出（不得概括描述）
  - 未完成项、遗留风险、下一步建议
停止条件：
  ■ 遇十一章任意"停止并询问"情形时立即停，不得自作取舍
  ■ 若发现需要突破本契约"禁止事项"，立即返回说明理由，不得先改再报
━━ END ━━
```

**回传结果（子 agent 写，主 agent 验）必须按上述 5 项交付物结构答复；不符合结构的结果主 agent 有权打回重做。**

---

## 十、对外贡献规则（PR/patch/issue）

1. **先读贡献规则**：提交前读当前仓库贡献指南、PR 模板、已有 open/closed PR、issue 和维护者反馈。
2. **确认问题真实存在**：必须是用户真实遇到的或当前代码可复现的问题；禁止用理论审查、猜测或编造的问题描述发起贡献。
3. **一次只解决一个问题**：禁止混入无关改动、顺手重构、格式化风暴或项目私货。
4. **不塞进通用核心**：只服务特定团队/领域/工具/fork/个人习惯的变更，应放到插件、扩展、配置或私有层。
5. **提交前向用户说明**：完整 diff 范围、验证证据和剩余风险，得到明确确认后再发布。

---

## 十一、必须停止并询问的情况

遇到以下情况必须停止，**不能只说"无法继续"**，必须给出：**冲突证据 + 可选方向 + 推荐方向 + 需要确认的问题**。

1. 用户需求、当前代码、宪法、真源文档、验收结果**互相冲突**。
2. 需要无意义兼容、保留旧产品残影或**同时支持互斥路线**。
3. 需要创建/改写/废弃宪法、真源索引、公开文档、产品命名、核心 API、schema、权限模型、部署流程。
4. 需要删除旧 API、字段、路由、配置、缓存、生成合同、部署脚本或迁移历史。
5. 当前证据表明用户目标会**损害架构、数据、安全、权限、用户体验或长期可维护性**。
6. 上下文不足以判断 owner 边界，且猜测会造成**不可逆或大范围影响**。

---

## 十二、交接规则

上下文过大、需要换窗口、交给另一个 agent、或任务尚未闭合时，必须产出**可直接复制的交接文本**。**禁止用自然语言概括代替下述结构化模板。**

> 灵感来源：Superpowers handoff 规范，融合本宪法 Git / 验收 / 停止规则

**交接文本（复制以下模板填充，缺一不可）：**

```
━━━━ 交接文本 v1 ━━━━
【1/6 任务锚点】
  会话 ID：          {原对话会话标识或日期}
  本轮目标：         {一句话说明本轮要交付的事}
  所属施工阶段：     {project/IMPLEMENTATION_PLAN.md 第 N 阶段 / 第 N 关卡}
  已确认边界：       {已向用户确认并锁定的产品/架构/常量边界}

【2/6 Git 状态快照】
  git root：         {绝对路径}
  status --short：   {原样粘贴 git status --short 输出，不要概括}
  staged files：     {已暂存文件列表}
  未提交用户改动：   {用户手动改了未 commit 的文件，需要保留的}
  嵌套仓库：         {是否存在子仓；若有，列子仓路径与状态}

【3/6 已完成工作（逐条对应验收证据）】
  ✓ 任务点 A：{一句话描述完成内容}
     → 验收命令：{实际跑的命令}
     → 原始输出：{粘贴关键输出片段或"见附件 N"，不得写"通过"}
  ✓ 任务点 B：…
  ✗ 未完成点：{若有，说明原因与阻塞原因}

【4/6 文档回写】
  daima/AGENT_CONSTITUTION.md：{是否变更，若有写变更摘要}
  daima/word/*.md：            {若有修改，列文件 + 修改的小节号}
  daima/project/*.md：         {若有修改，列文件 + 修改的小节号}
  其他：                       {README / docs/ 等}

【5/6 未闭合风险 & 禁止触碰项】
  ⚠ 风险 1：{未验证/边界不清/跨模块/可能漂移的点}
     → 严重度：{阻断 / 设计风险 / 债务}
     → 建议入口：{下一轮从哪里开始收}
  ⚠ 风险 2：…
  ✋ 禁止触碰：{本轮发现但明确不动的用户私有改动/配置/文件}

【6/6 下一步指引（接手 agent 直接照着做）】
  最安全首命令：  {例如"cd d:\\vscode\\momery2 && tsc --noEmit 先验证基线无错"}
  第一步动作：    {具体要做的第一件事，1-3 句，不要含糊}
  验收通过线：    {做到什么程度算"本轮真的完成"，列命令+断言}
  停止条件：      {遇到第十一章什么情形必须停，或遇到用户未授权的范围时停}
━━━━ END ━━━━
```

**交接铁律：**
- 2/6 的 `git status --short` 必须**原样粘贴原始输出**，不得写"干净"或"只有 X 文件改动"这类概括。
- 3/6 的"原始输出"不得缺失；没跑的验收命令就老实写 `✗ 未执行（原因）`。
- 接手 agent 的第一动作必须按 6/6 的"最安全首命令"先跑基线，**不准直接继续改代码**。
- 若交接缺失任一大段，接手 agent 有权回传"补全再续"，不承担跳步责任。

---

## 十三、@@STACK@@ 通用适配

| 标识 | 规则 |
|------|------|
| `FRAMEWORK` | 遵循框架生命周期、目录约定、状态管理、错误处理；禁止私造并行体系 |
| `LANGUAGE` | 代码风格、类型系统、包管理、lint、format、并发模型符合当前语言生态 |
| `UI` | 复用现有组件、设计 token、布局、交互模式和可访问性约束；禁止另起审美方向 |
| `DATA` | 数据库/schema/迁移/索引/事务/权限以数据层真源为准；禁止业务层伪造数据合同 |
| `AUTH` | 鉴权授权必须在服务端边界执行；不得只依赖页面、路由或前端状态 |
| `TEST` | 核心合同用单元/契约测试，跨入口用集成测试，UI 用浏览器验收，发布用构建+端到端门禁 |
| `DEPLOY` | 部署/打包/生成物通过项目既有命令生成验证；禁止手改生成物跳过生成器 |
| `RESOURCE` | 资源治理明确预算、上界、背压、超时、重试、idle-stop 和观测口径 |
| `PERFORMANCE` | 性能优化必须先定位真实瓶颈；不得牺牲正确性和权限边界 |

---

## 十四、项目适配块

### @@ADAPTER:framework/express-4@@
applies_when: "项目使用 Express 4"
verification: "tsc --noEmit; npm run build"

**四层架构红线**：

```
请求 → 中间件 → 路由 → 控制器 → 服务 → 数据访问 → 数据库
```

| 层 | @@OWNER@@ | 禁止行为 |
|----|----------|---------|
| routes | 路径注册 | 参数校验、写 SQL、访问数据库 |
| controllers | 参数提取+校验+响应 | 拼 SQL、写复杂业务判断 |
| services | 核心业务逻辑 | 访问 req/res、直接写 SQL |
| repositories | 纯 SQL 操作 | 业务逻辑、校验参数、调用其他 repo |
| middlewares | 横切关注点 | 操作业务数据、修改响应体结构 |

- 禁止跨层 import、循环引用；非 routes/ 目录禁止 `Router()`
- 响应通过 `utils/response.ts`（success/created/fail），禁止直接 `res.json()`
- async handler 必须用包装器捕获异常
- 新增模块七步走：类型 → repo → service → controller → route → 注册 → 前端
- 文件大小：controller≤80 / service≤120 / repository≤100 / route≤40 行

### @@ADAPTER:database/sqlite-better-sqlite3@@
applies_when: "项目使用 better-sqlite3"
verification: "PRAGMA table_info; EXPLAIN QUERY PLAN"

- 单例 `getDb()`；WAL + 外键 + busy_timeout 必配
- 所有 SQL `?` 参数化，禁止字符串拼接；禁止 repo 外写 SQL
- 8 表 + 13 索引按 DB §5.2 依赖序迁移
- 复习提交/笔记转卡/导入必用 `db.transaction()`

| 操作 | 级联行为 |
|------|---------|
| 删牌组 | 卡片+复习记录级联删，exams.deck_id 置 NULL |
| 删笔记 | 附件级联删，cards.source_note 置 NULL |
| 删文件夹 | 非空禁止（40902） |
| 删默认模板 | 禁止（40903） |

### @@ADAPTER:framework/vue-3@@
applies_when: "项目使用 Vue 3 + Composition API"
verification: "vue-tsc --noEmit; npm run build"

- 组件：`<script setup lang="ts">`、Props 有类型、`<style scoped>` + BEM、单文件≤200 行
- 5 个 Pinia Store（deck/review/note/ui/stats），Store 间不互引，API 在 api 模块层
- 扁平路由，`meta.tabGroup` 控 SubTabs，侧边栏按路径推断激活
- 共享类型集中 `src/types/index.ts`，字段沿用 snake_case 不做映射
- 样式全引 CSS 变量，禁止 `!important`，禁止内联样式（动态尺寸除外）

### @@ADAPTER:language/typescript@@
applies_when: "项目使用 TypeScript"
verification: "tsc --noEmit"

- 所有变量/函数/参数必须显式类型（立即调用箭头函数除外）
- 禁止 `any`（express 中间件兼容除外，加注释豁免）
- 禁止 `as` 断言（用类型守卫；API 响应处理允许 `as` + 注释）
- 接口优先于 type（模型用 interface，联合/交叉用 type）
- 禁止 `export default`（全命名导出）；禁止 callback（统一 async/await）
- 文件命名 `kebab-case.ts`，类型文件 `types.ts`

### @@ADAPTER:toolchain/winston-logger@@
applies_when: "项目使用 winston"
verification: "检查 logs/error.log + logs/combined.log"

- 禁止 `console.log`（统一 logger）
- 禁止记录密码/token/密钥/完整文件内容
- 每请求记录 method + url + status + duration

### @@ADAPTER:platform/multer-upload@@
applies_when: "项目使用 multer"
verification: "上传 7 种 MIME 类型验证"

- 白名单 7 种 MIME、单文件≤10MB、按月分目录
- 文件名 uuid 前缀脱敏
- 附件通过 `express.static(uploadDir)` 挂载 `/uploads`

---

## 十五、冻结常量（禁止改动）

### API 统一响应

```typescript
{ code: 0, message: "ok", data: T }                    // 成功
{ code: number, message: string, data: null }          // 失败
```

### 错误码注册表

| 段 | 范围 | 用途 |
|----|------|------|
| 400xx | 40001-40005 | 参数校验 |
| 404xx | 40401-40405 | 资源不存在 |
| 409xx | 40901-40903 | 业务冲突 |
| 500xx | 50001-50004 | 服务端错误 |
| 401xx | 预留（M2） | 未认证 |

> 未知异常兜底 = 50004 + 脱敏消息。禁止自创错误码。

### SM-2 算法

| 评分 | repetitions | interval | ease_factor |
|------|-------------|----------|-------------|
| forgot | 重置 0 | 10 分钟 | -0.20 |
| hard | 重置 0 | 1 天 (1440) | -0.15 |
| good | +1 | × ease_factor | 不变 |
| easy | +1 | × ease_factor × 1.3 | +0.15 |

**边界**：ease_factor 下限 1.3、interval 上限 525600 分钟（365 天）、首次复习 good→1440、easy→5760

### 统计口径

- 已掌握：`repetitions >= 3` 且 `interval >= 30240 分钟（21 天）`
- 正确率：`(good + easy) / 总评分次数`
- 连续学习天数：从今天倒推连续有复习记录的天数

### 禁止引入的依赖

ORM（Prisma/TypeORM/Drizzle）、zod/joi、express-validator、bcrypt/jsonwebtoken（M2）、Redis、Tailwind、UI 组件库（Element Plus/Naive UI）、ECharts/Chart.js

---

> **记忆口诀**：架构优先不补丁，真源分层不混写，推理闸闭再编码，设计安全不越界，验收匹配风险面，警告漂移皆缺陷，git 精确不加点，停询证据加方向，交接六格闭链路。

---

## 附录 A：自定义技能最佳实践（非强制执行，创建 skill 时参考）

> 灵感来源：Superpowers `writing-skills` + `using-superpowers`。
> 适用场景：当你想为本项目（记忆学习平台）创建专属的 agent skill 时参考。

### A.1 什么时候值得创建一个 skill

**值得创建的信号（满足 ≥2 条）：**
- 某类任务**反复出现 ≥3 次**，且每次都要查同一套规则（如 SM-2 结果复查、数据库表一致性检查、宪法红线审计）。
- 操作有**明确的输入/输出/停止条件**，能装进一个模板。
- 做错的**代价很高**（如误删 migration、改错 SM-2 常量、错误码冲突），需要 skill 作为强制检查点。
- **新手 agent 接手时会漏掉**的固定流程（如 9 阶段验收关卡核对）。

**不要创建 skill 的情况：**
- 一次性任务（如"初始化脚手架"），做过就不再需要。
- 纯知识查询类（没有固定动作，只有判断建议）。不如直接写进 daima/word/ 文档。
- 规则本身尚不稳定，每次都会大改 → 先写进宪法正文，稳定后再抽 skill。

### A.2 Skill 骨架（建议模板）

```markdown
# Skill：{技能名，动词开头，如"审查 SM-2 调度正确性"}

## 触发时机
{用户说了什么 / 上下文出现什么信号时启动本 skill。
 例：当用户请求"复习这个卡片的下一次调度是否正确"，
 或 agent 自己即将修改 review.service.ts 中 calcNextReview() 时}

## 输入要求
{本 skill 执行前必须拿到的信息，拿不到就询问，不要猜：
 - 卡片当前的 repetitions / ease_factor / interval / last_reviewed
 - 用户给出的评分（forgot / hard / good / easy）
 - 期望得到的 next_review / new_interval / new_ease_factor（若有）}

## 执行步骤（编号，不得跳步）
1. …
2. …
3. …

## 判定矩阵 / 红线
{列出必须通过的断言，一条不过即判定 FAIL。
 例：good 评分时 interval = old_interval × ease_factor
 例：ease_factor 不得 < 1.3
 例：interval 不得 > 525600 分钟}

## 输出格式
{PASS / FAIL + 理由 + 建议下一步。
 FAIL 时必须指出"第几步 / 哪条断言 / 实际值 vs 期望值"。}

## 与宪法的关系
本 skill 执行时仍须遵守 AGENT_CONSTITUTION.md 所有正文规则；
若有冲突，以宪法正文为准，本 skill 不具备超越正文的效力。
```

### A.3 本项目建议优先开发的 3 个专属 skill

| 优先级 | Skill 名 | 解决的痛点 |
|--------|---------|-----------|
| P1 | **SM-2 计算结果审计器** | 任何人改了复习调度逻辑后，自动跑 4 类评分 × 首复现/N次复现的边界断言，防止 SM-2 常量被无意漂移 |
| P2 | **数据库迁移一致性检查器** | 新建 migration 后，自动核对：8 表是否齐全、13 索引是否存在、外键/级联行为是否符合 第十四章级联表、PRAGMA foreign_key_check 通过 |
| P3 | **9 阶段施工关卡核对器** | claim 某阶段完成时，自动对照 IMPLEMENTATION_PLAN.md 的出口关卡，逐条过"是否有对应代码 / 是否有对应验收命令输出 / 是否有文档回写" |

### A.4 创建流程

1. 按 A.1 判断"是否值得创建"，不值得就放弃。
2. 按 A.2 骨架写 skill 草稿，**至少跑 1 次真实场景验证它真的能发现问题或产出正确结果**。
3. 在本附录 A.3 表格里登记（若它是 P1/P2/P3），或在下面"其他 skill 索引"登记。
4. Skill 不得直接修改宪法正文；若发现 skill 规则与宪法冲突，先改宪法正文并升级版本号，再改 skill。

> 其他 skill 索引：（本项目后续 skill 登记在这里，一行一个：名称 + 用途 + 文件位置）
